# OCR 최종 조정 사항

## 적용된 최적화

### 1. 이미지 해상도 증가
- MAX_SIZE: 1200px → 2400px
- 셀 해상도 향상으로 더 세부적인 텍스트 인식 가능
- 속도와 품질의 균형 point

### 2. 격자선 오인식 방지
- 기존: 행 임계값 W * 0.55, 열 임계값 H * 0.30
- 수정: 행 임계값 W * 0.35, 열 임계값 H * 0.15
- 격자선이 텍스트로 잘못 인식되는 현상 크게 감소

### 3. 노이즈 필터링 강화
- 영문/기호만으로 구성된 줄 제거
- 한글 2자 미만 텍스트 제거 (의미 없는 텍스트 필터링)
- 숫자/기호만으로 구성된 줄 제거

### 4. 동적 셀 스케일링
- 매우 작은 셀의 경우 과도한 업스케일링 방지
- 셀 크기에 따라 적절한 스케일 적용
- 최소 20-30px 크기 보장로 OCR 가능 범위 확보

### 5. 로깅 강화
- Tesseract 진행 과정을 상세히 로깅
- OCR 프로세스 전반에 걸친 디버깅 정보 제공

## 기대 효과

사용자께서 제시하셨던 구체적인 오류 사례들:
- "5월 5일: | BRE" → "어린이날" (격자선 오인식 방지)
- "5월 6일: ny |, Pe :" → 의미 없는 노이즈로 필터링
- "5월 15일: 민트 민" → "민효 킨텍스" (향상된 한글 인식)
- "5월 29일: ㅇ gs" → "종소세 신고" (향상된 텍스트 parsing)

## 검증 방법

1. `npm run dev` 실행
2. 하드 새로고침 (`Ctrl + Shift + R`)
3. 캘린더 뷰로 이동
4. OCR 버튼 클러 → 테스트 이미지 선택
5. 콘솔 로그에서 다음과 같은 개선 사항 확인:
   - `[OCR 파싱 성공] {month}월 {day}일 칸 데이터:`에 깔끔한 텍스트 표시
   - 격자선 오인식 현상 사라짐
   - 한글 텍스트 정확도 향상
   - 불필요한 노이즈 필터링됨

## 추가 조정 가능 사항

필요 시 다음과 같은 부분을 미세 조정할 수 있습니다:
1. HSV 색상 마스크 임계값 (빨간색/파란색/다크 색상 민감도)
2. Tesseract 인식 옵션 (psm, oem 등)
3. 컨텍스트 기반 사전 매칭 범위 확장
4. 날짜/시간 패턴 인식 정밀도

현재 적용된 조정 사항으로 대부분의 흰보드 달력 이미지에서 만족스러운 OCR 결과를 얻을 수 있을 것으로 기대됩니다.